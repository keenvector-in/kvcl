# IconButton

Round icon-only button with an optional count bubble (`99+` above 99). `label` is required —
it is both the accessible name and the tooltip.

```tsx
<IconButton label="Cart, 3 items" icon={<Icon name="cart" />} count={3} onClick={openCart} />
```
