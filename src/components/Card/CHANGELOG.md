# Card changelog

## 1.2.0 — 2026-09-20

- Merged with KeenPlaza's card: optional `title` + `actions` header row and `padded` (default true). A card without a title renders exactly as before.

## 1.1.0 — 2026-09-15

- Styled with semantic tokens (`fg`, `surface`, `line`) so it renders on light pages (`data-kv-mode="light"`) and follows a tenant `ThemeProvider`.

## 1.0.0

- Initial release.
