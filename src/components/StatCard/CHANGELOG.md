# StatCard changelog

## 1.2.0 — 2026-09-20

- `accent` renders its own brand-filled surface: as a Card className it lost to the card's own background and the white text became invisible in light mode.

## 1.1.0 — 2026-09-20

- Merged the KeenPlaza library's `kpi` component in: optional `delta`, `accent` and
  `loading` props; `label` and `value` widened from `string` to `ReactNode`.

## 1.0.0 — 2026-09-15

- Initial release.
