# Badge

Small status/label pill. Use `tone` to signal meaning — `brand` for highlights, `accent`
for positive/connected states, `warning` for attention, `neutral` otherwise.

```tsx
import { Badge } from '@keenvector/kvcl';

<Badge tone="accent">Connected</Badge>
<Badge tone="warning">Configuration required</Badge>
```
