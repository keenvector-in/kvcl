# PageHeader

Title row every console page starts with.

```tsx
<PageHeader title="Workflows" description="Automated journeys." actions={<Button>Create</Button>} />
```

| Prop | Type | Notes |
|---|---|---|
| `title` | `string` | |
| `description` | `string?` | |
| `actions` | `ReactNode?` | Right-aligned controls |
