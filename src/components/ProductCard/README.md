# ProductCard

Product tile for grids and rails; falls back to deterministic placeholder art.

```tsx
<ProductCard id="p1" title="Cotton kurta" eyebrow="Fabindia" footer={<PriceTag priceMinor={149900} />} href="/p/p1" />
```
