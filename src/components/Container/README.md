# Container

Page-content wrapper: centers children at a max width with consistent horizontal padding.
Use it once per page section, not nested.

```tsx
import { Container } from '@keenvector/kvcl';

<Container>{/* page content */}</Container>
```
