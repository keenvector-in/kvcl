# TenantIdDisplay

Shows the tenant the page is operating in. The ID is selectable so it can be pasted into a
support ticket; nothing here selects or infers a tenant.

```tsx
<TenantIdDisplay tenantId={tenantId} />
<TenantIdDisplay tenantId={tenantId} variant="inline" />
```
